import UIKit

/// Performs OCR on ink snapshot images using the Vision framework.
/// V0.1: stub. Future: VNRecognizeTextRequest integration.
enum VisionOCRService {

    static func recognizeText(in image: UIImage) async throws -> String {
        // V0.1 stub — future: Vision framework OCR
        return ""
    }
}
