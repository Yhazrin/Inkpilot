import UIKit
import PencilKit

/// Renders a snapshot image of a region of ink for OCR processing.
/// V0.1: stub. Future: render PKDrawing region to UIImage for Vision OCR.
enum InkSnapshotRenderer {

    static func render(drawing: PKDrawing, in rect: CGRect) -> UIImage? {
        // V0.1 stub — future: render ink region to image
        return nil
    }
}
